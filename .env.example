# Database connection
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=claude_logs
DB_USER=claude_admin
DB_PASSWORD=your-secure-password

# Optional: Override collector host identifier (defaults to hostname)
# COLLECTOR_HOST=my-machine-name
